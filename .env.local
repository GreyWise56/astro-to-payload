# .env.local (create this in your Astro project root)
# Payload CMS API Configuration

PAYLOAD_API_URL=http://localhost:3000/api

# Optional: If you want to use a different port for production
# PAYLOAD_API_URL=https://your-production-domain.com/api